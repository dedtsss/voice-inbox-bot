services:
  voice-inbox-bot:
    build: .
    container_name: voice-inbox-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
